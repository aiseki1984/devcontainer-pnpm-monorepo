{
  "name": "pnpm-test-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.3",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "dotenv -- turbo dev",
    "dev:api": "dotenv -- turbo dev --filter @pnpm-test-workspace/api",
    "build": "dotenv -- turbo build",
    "lint": "turbo lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "turbo typecheck",
    "test": "dotenv -- turbo test",
    "db:generate": "dotenv -- pnpm --filter @pnpm-test-workspace/db db:generate",
    "db:migrate": "dotenv -- pnpm --filter @pnpm-test-workspace/db db:migrate",
    "db:seed": "dotenv -- pnpm --filter @pnpm-test-workspace/db db:seed",
    "db:studio": "dotenv -- pnpm --filter @pnpm-test-workspace/db db:studio",
    "db:reset": "dotenv -- bash scripts/db-reset.sh",
    "ci": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm build",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,mjs,cjs,ts,tsx,json,md,yml,yaml,css}": "prettier --write"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "prettier": "^3.8.4",
    "turbo": "^2.9.18",
    "typescript": "^6.0.3"
  }
}
